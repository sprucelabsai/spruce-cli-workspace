{
	"version": "0.0.1",
	"private": true,
	"name": "spruce-cli-workspace",
	"description": "The DX tools used to build next generation experiences on the Spruce Experience Platform. 💻",
	"license": "MIT",
	"workspaces": [
		"packages/spruce-templates",
		"packages/spruce-cli"
	],
	"engines": {
		"node": ">=12",
		"npm": ">=6"
	},
	"scripts": {
		"lint": "lerna run lint --parallel",
		"lint.tsc": "lerna run lint.tsc --parallel",
		"lint.fix": "lerna run lint.fix --parallel",
		"rebuild": "yarn clean.all && yarn && yarn build && yarn cache.tests",
		"build": "lerna run build",
		"build.types": "lerna run build.types",
		"test": "lerna run test --parallel --",
		"watch.build": "lerna run watch.build --stream --parallel",
		"watch.lint": "lerna run watch.lint --stream --parallel",
		"watch.tests": "lerna run watch.tests --parallel",
		"watch.rebuild": "yarn clean.all && yarn && yarn watch.build",
		"publish": "lerna publish",
		"clean": "lerna run clean --parallel",
		"clean.cache": "lerna run clean.cache --parallel",
		"cache.tests": "lerna run cache.tests --parallel",
		"clean.all": "yarn clean.cache && rm -f yarn.lock && lerna run clean && rm -rf node_modules; for d in packages/*/node_modules; do echo $d; rm -rf $d; done && yarn clean.build",
		"clean.build": "for d in packages/*/build; do echo $d; rm -rf $d; done",
		"upgrade.packages": "yarn upgrade --latest && rm -f yarn.lock && yarn | true && yarn lint.fix || true",
		"upgrade.packages.workspaces": "yarn && for d in packages/*; do cd $d; yarn-upgrade-all; cd ../..; done",
		"upgrade.packages.all": "yarn install && yarn upgrade.packages.workspaces && yarn upgrade.packages",
		"upgrade.packages.test": "yarn upgrade.packages.all && yarn lint && yarn build && yarn test"
	},
	"dependencies": {},
	"devDependencies": {
		"@babel/cli": "^7.12.1",
		"@babel/core": "^7.12.3",
		"@babel/plugin-proposal-class-properties": "^7.12.1",
		"@babel/plugin-proposal-decorators": "^7.12.1",
		"@babel/plugin-transform-runtime": "^7.12.1",
		"@babel/preset-env": "^7.12.1",
		"@babel/preset-typescript": "^7.12.1",
		"@sprucelabs/babel-plugin-schema": "^3.1.2",
		"babel-plugin-module-resolver": "^4.0.0",
		"conventional-changelog-sprucelabs": "^1.1.2",
		"eslint": "^7.11.0",
		"eslint-config-spruce": "10.9.125",
		"lerna": "^3.20.2",
		"typescript": "^4.0.3",
		"yarn-upgrade-all": "^0.5.4"
	}
}
