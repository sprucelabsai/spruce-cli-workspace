// a mixin of all options for all the errors we can through
{{#each options}}
import { I{{pascalName}}ErrorOptions } from "./{{camelName}}.types";
{{/each}}
import { SpruceErrorOptions } from "@sprucelabs/error";
import {SchemaErrorOptions} from '@sprucelabs/schema'

export type ErrorOptions = SchemaErrorOptions | SpruceErrorOptions{{#each options}} | I{{pascalName}}ErrorOptions{{/each}}