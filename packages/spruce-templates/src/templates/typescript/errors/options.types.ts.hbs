import { SpruceErrors } from "#spruce/errors/errors.types"
import { SpruceErrorOptions, ISpruceErrorOptions} from "@sprucelabs/error"
import { SchemaErrorOptions } from '@sprucelabs/schema'

{{#each options}}
interface I{{namePascal}}ErrorOptions extends SpruceErrors.{{namespace}}.I{{namePascal}}, ISpruceErrorOptions {
	code: '{{code}}'
}
{{/each}}

type ErrorOptions = SchemaErrorOptions | SpruceErrorOptions{{#each options}} | I{{namePascal}}ErrorOptions {{/each}}

export default ErrorOptions
