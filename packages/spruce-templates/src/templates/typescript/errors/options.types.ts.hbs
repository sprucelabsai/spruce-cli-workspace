import { SpruceErrors } from "#spruce/errors/errors.types"
import { SpruceErrorOptions, ErrorOptions as ISpruceErrorOptions} from "@sprucelabs/error"
import { SchemaErrorOptions } from '@sprucelabs/schema'

{{#each options}}
export interface {{namePascal}}ErrorOptions extends SpruceErrors.{{namespace}}.{{namePascal}}, ISpruceErrorOptions {
	code: '{{code}}'
}
{{/each}}

type ErrorOptions = SchemaErrorOptions | SpruceErrorOptions{{#each options}} | {{namePascal}}ErrorOptions {{/each}}

export default ErrorOptions
