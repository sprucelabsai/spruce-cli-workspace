// throwing your new error
import SpruceError from '../errors/Error'
import { ErrorCode } from '../.spruce/errors/codes.types'

// throw your error
throw new SpruceError({
    code: ErrorCode.{{pascalName}},
    {{#each definition.fields}}
    {{@key}}: ...,
    {{/each}}
})