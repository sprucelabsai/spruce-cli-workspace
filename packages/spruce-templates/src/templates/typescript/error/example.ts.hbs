// import your error
import SpruceError from '../errors/Error'

// throw your error
throw new SpruceError({
    code: '{{nameConst}}'',
    {{#each schema.fields}}
    {{@key}}: ...,
    {{/each}}
})
