import { {{globalNamespace}} } from '{{typesFile}}'
import FieldType from '#spruce/schemas/fields/fieldTypeEnum'

{{{ importRelatedDefinitions definition }}}

const {{nameCamel}}Definition: {{globalNamespace}}.{{namespace}}.{{namePascal}}{{#if definition.version}}.{{definition.version}}{{/if}}.IDefinition  = {
	{{> schemaDefinition namespace=namespace schema=definition renderAs="value" }}
}

export default {{nameCamel}}Definition
