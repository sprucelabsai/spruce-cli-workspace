import * as SpruceSchema from '@sprucelabs/schema'
import Schema, { ISchemaDefinition } from '@sprucelabs/schema'

/**
import {{camelName}}Definition from '{{relativeToDefinition}}'
type {{pascalName}}Definition = typeof {{camelName}}Definition
*/

export interface I{{pascalName}}Definition extends ISchemaDefinition {
	{{> schemaDefinition schema=definition renderAs="type" }}
}

export const {{camelName}}Definition : I{{pascalName}}Definition = {
	{{> schemaDefinition schema=definition renderAs="value" }}
}

// {{description}}
export interface I{{pascalName}} {
	{{>schemaValues definition}}
}
export interface I{{pascalName}}Instance extends Schema<I{{pascalName}}Definition> {}
