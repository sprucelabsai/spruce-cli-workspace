/* eslint-disable @typescript-eslint/no-namespace */
/* eslint-disable no-redeclare */
/* eslint-disable no-unused-vars */
/* eslint-disable @typescript-eslint/no-unused-vars */

import { default as SchemaEntity } from '@sprucelabs/schema'
export { SpruceSchemas } from '@sprucelabs/spruce-core-schemas/build/.spruce/schemas/schemas.types'

{{#each imports}}
import * as {{importAs}} from '{{package}}'
{{/each}}


declare module '@sprucelabs/spruce-core-schemas/build/.spruce/schemas/schemas.types' {

	{{#each schemaTemplateItems}}

	namespace {{../globalNamespace}}.{{namespace}}{{#if (isDefined schema.version)}}.{{schema.version}}{{/if}} {

		{{#if schema.description}}/** {{schema.description}} */{{/if}}
		interface I{{namePascal}} {
			{{> schemaValues schema=schema renderAs="type" }}
		}

		interface I{{namePascal}}Schema extends SpruceSchema.ISchema {
			{{> schemaBody namespace=namespace schema=schema renderAs="type" }}
		}

		type {{namePascal}}Entity = SchemaEntity<{{../globalNamespace}}.{{namespace}}{{#if (isDefined schema.version)}}.{{schema.version}}{{/if}}.I{{namePascal}}Schema>

	}

	{{/each}}
}
