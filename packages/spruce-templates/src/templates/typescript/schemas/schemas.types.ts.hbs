/* eslint-disable @typescript-eslint/no-namespace */
/* eslint-disable no-redeclare */

import { default as Schema } from '@sprucelabs/schema'
{{#each imports}}
import * as {{importAs}} from '{{package}}'
{{/each}}

import FieldType from '#spruce/schemas/fields/fieldTypeEnum'

{{#each schemaTemplateItems}}

export declare namespace {{../globalNamespace}}.{{namespace}}{{#if (isDefined definition.version)}}.{{definition.version}}{{/if}} {

	/** {{definition.description}} */
	export interface I{{namePascal}} {
		{{> schemaValues schema=definition renderAs="type" }}
	}

	/** The interface for the schema definition for a {{nameReadable}} */
	export interface I{{namePascal}}Definition extends SpruceSchema.ISchemaDefinition {
		{{> schemaDefinition namespace=namespace schema=definition renderAs="type" }}
	}

	/** The type of a schema instance built off this definition */
	export type {{namePascal}}Instance = Schema<{{../globalNamespace}}.{{namespace}}.{{#if (isDefined definition.version)}}.{{definition.version}}{{/if}}.I{{namePascal}}Definition>

}

{{/each}}



