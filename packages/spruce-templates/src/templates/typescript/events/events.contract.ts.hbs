import { buildEventContract } from '@sprucelabs/mercury-types'

const eventsContract = buildEventContract({{{ eventContract }}})
export default eventsContract

export type EventsContract = typeof eventsContract