import AbstractSpruceTest, { test, assert } from '@sprucelabs/test'

export default class {{namePascal}}Test extends AbstractSpruceTest {

	@test('Tests can accept vars', 'hello', 'world')
	protected static async exampleWithVars(str1: string, str2: string) {
		assert.isEqual(str1, 'hello')
		assert.isEqual(str2, 'world')
	}


	@test()
	protected static async {{nameCamel}}() {
		assert.isTrue(false)	
	}
}
