import Schema, {
	SchemaDefinitionValues
} from '@sprucelabs/schema'

import {{camelName}}Definition from '{{relativeToDefinition}}'

export type {{pascalName}}Definition = typeof {{camelName}}Definition
export interface I{{pascalName}} extends SchemaDefinitionValues<{{pascalName}}Definition> {}
export interface I{{pascalName}}Instance extends Schema<{{pascalName}}Definition> {}
