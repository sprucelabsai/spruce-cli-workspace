{{#each options}}
import { {{pascalName}} } from "./{{camelName}}.types";
{{/each}}
import { SpruceErrorOptions } from "@sprucelabs/error";
import {SchemaErrorOptions} from '@sprucelabs/schema'

export type ErrorOptions = SchemaErrorOptions | SpruceErrorOptions{{#each options}} |{{pascalName}}{{/each}}