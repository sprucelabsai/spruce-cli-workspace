import { AutoLoadable, Autoloaded } from '#spruce/autoloaders'
import { {{namePascalPlural}} } from '#spruce/autoloaders/{{nameCamelPlural}}'

export interface {{namePascal}}Options {
	/* Define your constructor options here */
}

/** {{description}} **/
export default abstract class Abstract{{namePascal}} implements AutoLoadable {
	public {{nameCamelPlural}}!: {{namePascalPlural}}

	public constructor(options: {{namePascal}}Options) {
		console.log(options)
	}

	/** invoked after everything is autoloaded */
	public async afterAutoload(autoloaded: Autoloaded) {
		this.{{nameCamelPlural}} = autoloaded.{{nameCamelPlural}}
	}
}
