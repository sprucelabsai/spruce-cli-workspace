{{#*inline "fieldstmpl"}}
{{@key}}: string
{{/inline}}

{{#each schemas}}

export namespace SpruceSchema.{{slug}}.{{id}} {
	export interface I{{startCase id}} {
		{{#each fields}}
			{{> fieldstmpl}}
		{{/each}}
	}

	export const schema = {{schemaStr}}
}

{{/each}}